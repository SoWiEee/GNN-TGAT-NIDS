name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: Lint (ruff)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # ruff doesn't need torch — use the standalone action.
      # Pass explicit paths so the repo root is not scanned as a fallback.
      - uses: astral-sh/ruff-action@v3
        with:
          args: "check src/ app/ tests/ scripts/ train.py"

  test:
    name: Test (CPU)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          version: "latest"

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      # Create venv, then pre-install CPU torch before uv sync so the CUDA
      # variant is never fetched. --no-install-package torch tells uv to skip it.
      - name: Create virtual environment
        run: uv venv

      - name: Install CPU PyTorch
        run: |
          uv pip install torch \
            --index-url https://download.pytorch.org/whl/cpu

      # Install everything else from the lockfile; skip torch (already installed).
      - name: Install project + dev dependencies
        run: uv sync --group dev --no-install-package torch

      # PyG C extensions — CPU wheel URL derived from the installed torch version.
      - name: Install PyG CPU extensions
        run: |
          TORCH_VER=$(uv run python -c "import torch; print(torch.__version__.split('+')[0])")
          uv pip install torch_scatter torch_sparse torch_cluster \
            --find-links "https://data.pyg.org/whl/torch-${TORCH_VER}+cpu.html"

      - name: Run tests
        run: uv run pytest --tb=short -q

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: .pytest_cache/
          retention-days: 7
